# Finance Tracker v2.0.2

## iPhone Home Screen icons

- Adds a Finance Tracker icon for the main application.
- Adds a separate Quick Entry icon when `/quick` is added to the iPhone Home Screen.
- Existing data is unchanged. Preserve the `data` folder when updating.

After updating, remove any old Home Screen shortcuts and add them again from Safari so iOS picks up the new icons.
